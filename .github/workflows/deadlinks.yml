name: Deadlinks
on:
  workflow_dispatch:
  
jobs:
  deadfinder:
    runs-on: ubuntu-24.04
    steps:
    - name: Run DeadFinder
      uses: hahwul/deadfinder@531e52b8e4e6a844132387502b85cb80efe3af13
      id: broken-link
      with:
        command: url
        target: https://www.hahwul.com
    
    - name: Output Handling
      run: echo ${{ steps.broken-link.outputs.output }}
