name: Deadlinks
on:
  workflow_dispatch:
  
jobs:
  deadfinder:
    runs-on: ubuntu-24.04
    steps:
    - name: Run DeadFinder
      uses: hahwul/deadfinder@99ca745a8443648e5c19c968c8e4d9a049fbdfcf
      id: broken-link
      with:
        command: url
        target: https://www.hahwul.com
    
    - name: Output Handling
      run: echo ${{ steps.broken-link.outputs.output }}
